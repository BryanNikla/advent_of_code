# Go

## To Run Code

Execute code using Go:

`go run .` or `go run main.go`

The program will then prompt for year and day

#### Examples:

`go run .`

`go run . -year 2025`

`go run . -year 2025 -day 5`
